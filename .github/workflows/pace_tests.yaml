name: "Unit tests"
on:
  push:
  pull_request:
      types: [opened, synchronize, reopened, ready_for_review, labeled, unlabeled]

jobs:
  pace_unit_tests:
    uses: mlee03/pace/.github/workflows/main_unit_tests_mpich.yaml@reusable_workflow
    with:
      ndsl_trigger:  true
