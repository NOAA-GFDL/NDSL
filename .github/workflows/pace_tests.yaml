name: "pace main tests"
on:
  push:
  pull_request:
      types: [opened, synchronize, reopened, ready_for_review, labeled, unlabeled]

jobs:
  pace_main_tests:
    uses: mlee03/pace/.github/workflows/main_unit_tests.yaml@reusable_workflow
    with:
      component_trigger:  true
      component_name: NDSL
